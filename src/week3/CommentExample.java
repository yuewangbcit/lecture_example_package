package src.week3;

/**
 * Short one line description about the class.
 * <p>
 * Longer description. If there were any, it would be here.
 * </p>
 * <p>
 * And even more explanations to follow in consecutive paragraphs
 * separated by HTML paragraph breaks.
 * </p>
 *
 * @author The author of the code
 * @version The version of the code
 */
public class CommentExample {
    // This is an inline comment used to describe the instance variable
    private int someVariable;

    /**
     * Short one line description about the method (start with a verb).
     * <p>
     * Longer description. If there were any, it would be here.
     * </p>
     * <p>
     * And even more explanations to follow in consecutive paragraphs
     * separated by HTML paragraph breaks.
     * </p>
     *
     * @param someParameter Description about the parameter.
     * @return Description about the return value.
     */
    public int someMethod(double someParameter) {
        // This is an inline comment used to explain a block of code
        return 0;
    }

    /*
     This is a comment that may across line breaks. It is great for
     commenting private methods that do not get Javadoc.
     */
    private void anotherMethod() {

    }
}
